<!DOCTYPE HTML>
<html>
    <head>
        <title>Grocery</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.2/css/bootstrap.min.css" integrity="sha384-PsH8R72JQ3SOdhVi3uxftmaW6Vc51MKb0q5P2rRUpPvrszuE4W1povHYgTpBfshb" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/main.css">
        <link href="https://fonts.googleapis.com/css?family=EB+Garamond|Quicksand" rel="stylesheet">
        <link rel="stylesheet" href="/font-awesome/css/font-awesome.min.css">
    </head>

    <body>
        
<nav class="navbar navbar-expand-lg">
  <a class="navbar-brand" href="/">Grocery</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class='navbar-nav mr-auto'>
            <% if(!currentUser) { %>
            <% } else { %>
                <li class="nav-item">
                    <a href="/users/<%= currentUser._id %>/recipes" class='nav-link'>Recipe Book</a>
                </li>
                <li class="nav-item">
                    <a href="#" style='cursor: not-allowed;' class='nav-link disabled' data-container="body" data-toggle="popover" data-placement="bottom" data-content="Coming soon!">Grocery List</a>
                </li>
                <li class="nav-item">
                    <a href='/users/<%= currentUser._id %>/recipes/new' class='nav-link'>Add a Recipe</a>
                </li>
                <li class="nav-item">
                    <a href='/users' class='nav-link'>Browse Users</a>
                </li>
            <% } %>
        </ul>
        <ul class="navbar-nav navbar-right">
            <% if(!currentUser) { %>
                <li class="nav-item">
                    <a href="/login" class='nav-link'>Log In</a>
                </li>
                <li class="nav-item">
                    <a href="/users/new" class='nav-link'>Sign Up</a>
                </li>
            <% } else { %>
                <li class="nav-item">
                    <a href='#' class='nav-link active'>Logged in as <%= currentUser.username %></a>
                </li>
                <li class="nav-item">
                    <a href="/logout" class='nav-link'>Log Out</a>
                </li>
            <% } %>
        </ul>
  </div>
</nav>

<% if(error && error.length > 0) { %> <!-- need .length because error exists but is undefined if no error -->
  <div class="container container-alert">
    <div class="alert alert-danger" role='alert'>
      <%= error %>
    </div>
  </div>
<% } %>

<% if(success && success.length > 0) { %>
  <div class="container container-alert">
    <div class="alert alert-success" role='alert'>
      <%= success %>
    </div>
  </div>
<% } %>